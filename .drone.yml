---
clone:
  depth: 1
kind: pipeline
name: AzurePipeline
steps:
  -
    commands:
      - "export token=$TOKEN"
      - "curl https://raw.githubusercontent.com/KazuDante89/lavender_lineage_kernel/Neuronic_v0.1/build.sh | bash"
    environment:
      TOKEN:
        from_secret: token
    image: panchajanya1999/archlinux:latest
    name: Azure-Images
    trigger:
      branch:
        - Neuronic_v0.1
